{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions for the primary PTConductor window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

